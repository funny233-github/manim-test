
name: test-scene
run-name: ${{ github.actor }} is test scenes
on:
  - push
jobs:
  run-class-TextScene:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt update
      - uses: actions/checkout@v4
      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - uses: FedericoCarboni/setup-ffmpeg@v2
        id: setup-ffmpeg
      - uses: openrndr/setup-opengl@v1.1
      - name: install requirements and manimgl
        run: sudo apt install texlive libpango1.0-dev -y ;
             pip3 install manimgl
      - name: test Scene TextScene
        run: python3 -m manimlib -uhd myAnimation.py TextScene -o
